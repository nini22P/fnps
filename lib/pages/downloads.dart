import 'package:flutter/material.dart';
import 'package:vita_dl/utils/pkg.dart';
import 'package:vita_dl/utils/storage.dart';

class Downloads extends StatelessWidget {
  const Downloads({super.key});

  @override
  Widget build(BuildContext context) {
    () async {
      // final downloadsPath = await getDownloadsPath();
      // final pkgPath = '$downloadsPath/1.pkg';
      // final pkgZipName = await getPkgZipName(pkgPath);
      // print('pkgName: $pkgZipName');
      // await pkg2zip(pkgPath: pkgPath);
    }();
    return const Center(
      child: Text('Todo'),
    );
  }
}
